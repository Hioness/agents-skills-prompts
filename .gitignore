# ── Scratch / temp ─────────────────────────────────────────────────
# `temp/` (no leading slash) matches every `temp` directory at any
# depth: root temp/, skills/*/temp/, forLoops/temp/, nested copies.
temp/

# ── Personal prompts ───────────────────────────────────────────────
chat-bg-prompts/

# ── Internal agent config ──────────────────────────────────────────
# AGENTS.md files hold internal configuration. They stay local and
# are not distributed with this repo. The README is the public face.
AGENTS.md

# ── Non-redistributable reference ──────────────────────────────────
# The ASD-STE100 standard (© ASD, all rights reserved) is reference
# material for the ste-writing skill. It stays local and is not
# uploaded. The linter and skill work without it.
skills/ste-writing/reference/

# ── Dependencies ───────────────────────────────────────────────────
node_modules/

# ── OS / editor junk ───────────────────────────────────────────────
.DS_Store
Thumbs.db
*.log
